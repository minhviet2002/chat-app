import { Box, Typography } from "@mui/material";

// const BHS = styled(Box)({
//     backgroundColor: '#cccc'
// })

function Logo() {

    return (
        <Box sx={{}}>
            <Typography variant="subtitle1" fontWeight={'bold'}>Dev Front End</Typography>
        </Box>
    );
}

export default Logo;